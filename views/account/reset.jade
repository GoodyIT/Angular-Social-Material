extends ../layout

block content
  .col-sm-8.col-sm-offset-2
    h1 Reset Password
    form(method='POST')
      input(type='hidden', name='_csrf', value=_csrf)
      input(type='hidden', name='token', value=token)
      input(type='hidden', name='userId', value=userId)
      .form-group
        label(for='password') New Password
        input.form-control(type='password', name='password', value='', placeholder='New password', autofocus=true)
      .form-group
        label(for='confirm') Confirm Password
        input.form-control(type='password', name='confirm', value='', placeholder='Confirm password')
      .form-group
        button.btn.btn-yellow-fill.btn-reset.pull-right(type='submit')
          i.fa.fa-keyboard-o
          | Change Password
